import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  UserImageContainer: {
    borderWidth: 1,
    borderColor: '#F35BAC',
    padding: 3,
    borderRadius: 50,
  },
});

export default style;
